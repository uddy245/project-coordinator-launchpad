# LES-001: Seed Lesson 20 + RAID rubric v1

**Milestone:** M3 Lesson Delivery
**Dependencies:** AUTH-004
**Estimated session length:** medium (30–90 min)
**Status:** not started

## Context

`lessons`, `rubrics`, and `prompts` tables already exist from the init migration. This ticket adds the first row to each: Lesson 20 (slug `raid-logs`), the v1 RAID rubric (5 dimensions), and the v1 grading prompt — all via a deterministic seed migration so every environment agrees on the starting content.

## Files to create

- `supabase/migrations/20260105_seed_lesson_20.sql` — idempotent INSERT ... ON CONFLICT for lesson + rubric + prompt rows
- `docs/rubrics/raid-v1.json` — canonical JSON (source of truth for the DB row)
- `docs/prompts/grade-raid-v1.md` — canonical prompt markdown (source of truth for the DB row)
- `tests/integration/lessons-rubrics-rls.test.ts` — confirms learners with `has_access=true` can read, others cannot

## Acceptance criteria

- [ ] `select * from lessons where slug = 'raid-logs'` returns one row with `is_published=true`
- [ ] `select * from rubrics where competency = 'risk_identification' and is_current = true` returns the v1 rubric with 5 dimensions in the JSON
- [ ] `select * from prompts where name = 'grade-raid' and is_current = true` returns the v1 prompt
- [ ] RLS: authenticated user with `has_access=true` can read; `has_access=false` cannot; admin can read all
- [ ] Running the migration twice is a no-op (ON CONFLICT)

## Tests required

- [ ] Integration: `tests/integration/lessons-rubrics-rls.test.ts`

## Definition of done

- [ ] Acceptance criteria checked
- [ ] Migration applied to prod Supabase via `supabase db push`
- [ ] `pnpm test` and `pnpm typecheck` and `pnpm lint` clean
- [ ] PR merged
