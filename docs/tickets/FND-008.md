# FND-008: README.md and verify CLAUDE.md

**Milestone:** M1 Foundation
**Dependencies:** FND-007
**Estimated session length:** medium (30–90 min)
**Status:** not started

## Context

The `CLAUDE.md` file already exists from the starter kit. This ticket verifies it's current (any new patterns from FND-001 through FND-007 should be reflected) and writes the project `README.md` for humans who clone the repo.

## Files to create/modify

- `README.md` (create) — project overview, local setup, deploy, key docs
- `CLAUDE.md` (review and update if anything from FND-001..FND-007 introduced a new pattern not captured)

## Acceptance criteria

- [ ] `README.md` covers: what the project is, the stack (link to ADR 0001), how to run locally (`pnpm install`, `supabase start`, env vars, `pnpm dev`), how to deploy (Vercel on merge to main), key docs (`BUILD_PLAN.md`, `CLAUDE.md`, `docs/tickets/`, `docs/adrs/`)
- [ ] `CLAUDE.md` is still under 150 lines
- [ ] `CLAUDE.md` reflects any new patterns introduced (e.g., the `@/env` import rule from FND-003)
- [ ] A new Claude Code session, reading only `CLAUDE.md`, can navigate the repo and understand the stack

## Definition of done

- [ ] Acceptance criteria checked
- [ ] A fresh Claude Code session has been tested: `/clear`, then prompt "What is this project and where do I start?" — the response should be accurate without needing the user to explain anything
- [ ] PR opened
