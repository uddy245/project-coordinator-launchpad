# PAY-001: Stripe product setup and checkout session action

**Milestone:** M2 Auth & Checkout
**Dependencies:** AUTH-004
**Estimated session length:** medium (30–90 min)
**Status:** not started

## Context

Create the Stripe product ($749 one-time, Launchpad Program) and a server action that creates a Checkout Session for the logged-in user. No webhook yet — PAY-002 handles access grant. The dashboard gate (PAY-003) consumes the CTA this ticket introduces.

Stripe API version is pinned; never use `apiVersion: 'latest'`.

## Questions to resolve before starting

- [ ] Which Stripe environment — test-mode for dev and preview, live-mode only on production? Default: yes. Live keys only in Vercel Production env.
- [ ] Manual step required: create the Stripe product + price in the Stripe dashboard and record the price ID in `STRIPE_PRICE_ID`.

## Files to create

- `src/lib/stripe/client.ts` — Stripe SDK instance with pinned `apiVersion`
- `src/actions/checkout.ts` — `createCheckoutSession` server action
- `src/app/(app)/checkout/success/page.tsx`
- `src/app/(app)/checkout/cancel/page.tsx`
- `src/components/marketing/purchase-cta.tsx` — the button rendered on dashboard
- `tests/unit/checkout-action.test.ts` — mocked Stripe call shape

## Acceptance criteria

- [ ] Logged-in user without access sees a `<PurchaseCTA />` on dashboard
- [ ] Clicking the CTA redirects to a Stripe Checkout page
- [ ] Success URL `/checkout/success?session_id={CHECKOUT_SESSION_ID}` renders a "thanks, activating your account" message
- [ ] Cancel URL redirects cleanly back to dashboard
- [ ] User with `has_access=true` does not see the CTA (dashboard shows lessons instead)
- [ ] Stripe API version is pinned in `src/lib/stripe/client.ts`

## Tests required

- [ ] Unit: `tests/unit/checkout-action.test.ts` — action input validation, Stripe client is called with the correct price ID, mode, and success/cancel URLs

## Definition of done

- [ ] Acceptance criteria checked
- [ ] `pnpm test` and `pnpm typecheck` and `pnpm lint` clean
- [ ] Stripe price ID added to Vercel Production and Preview env
- [ ] Manually verified: purchase completes on preview deploy using a test card (4242 4242 4242 4242)
- [ ] PR merged
