# FND-004: Vitest setup

**Milestone:** M1 Foundation
**Dependencies:** FND-001
**Estimated session length:** short (≤30 min)
**Status:** not started

## Context

Unit test framework. Keep it minimal — no Vitest UI, no coverage thresholds yet, no coverage reports. Just `pnpm test` that runs the suite against `src/lib/**` and `tests/unit/**`.

## Questions to resolve before starting

- None.

## Files to create

- `vitest.config.ts` — with path alias `@/*` matching tsconfig, React plugin for component tests later
- `tests/setup.ts` — global setup (extend expect with jest-dom matchers)
- `tests/unit/example.test.ts` — one passing test (delete after real tests exist)
- Update `package.json` `scripts`: `test`, `test:watch`

## Acceptance criteria

- [ ] `pnpm test` runs and exits 0 with the example test passing
- [ ] `pnpm test:watch` enters watch mode
- [ ] Path aliases (`@/*`) resolve in tests
- [ ] Tests can import from `src/lib/` (verify with a trivial import)

## Tests required

None beyond the example test.

## Definition of done

- [ ] All acceptance criteria checked
- [ ] Typecheck clean, lint clean
- [ ] PR opened
