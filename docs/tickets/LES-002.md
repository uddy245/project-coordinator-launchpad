# LES-002: Learner dashboard shell

**Milestone:** M3 Lesson Delivery
**Dependencies:** PAY-003, LES-001
**Estimated session length:** medium (30–90 min)
**Status:** not started

## Context

Replace the PAY-001 placeholder lesson card with a real dashboard showing: user greeting, four gate status badges (all "Pending" in MVP), and the Lesson 20 card with "Continue" / "Not started" / "Completed" state. Data comes from `profiles`, `lessons`, and `lesson_progress` (introduced in LES-004 — stub read returning `{}` for now).

## Files to create / modify

- `src/app/(app)/dashboard/page.tsx` — replace current content
- `src/components/dashboard/lesson-card.tsx` — progress-aware card
- `src/components/dashboard/gate-status-badge.tsx` — shows Pending / In progress / Complete
- `tests/e2e/dashboard.spec.ts`

## Acceptance criteria

- [ ] Dashboard shows `Welcome, <first_name_or_email>` greeting
- [ ] Lesson 20 card shows correct state — defaults to "Not started" when no progress row exists
- [ ] Four gate badges render with Pending state for MVP users
- [ ] Clicking the lesson card navigates to `/lessons/raid-logs`

## Tests required

- [ ] E2E: `tests/e2e/dashboard.spec.ts` — manual setup with a seeded has_access user

## Definition of done

- [ ] Acceptance criteria checked
- [ ] `pnpm test` and `pnpm typecheck` and `pnpm lint` clean
- [ ] PR merged
