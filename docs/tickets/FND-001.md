# FND-001: Initialize Next.js 15 + TypeScript + Tailwind + shadcn/ui

**Milestone:** M1 Foundation
**Dependencies:** none
**Estimated session length:** short (≤30 min)
**Status:** not started

## Context

Fresh repo. This ticket sets up the base Next.js 15 App Router project with TypeScript, Tailwind, shadcn/ui initialized, pnpm as the package manager, and the repo's placeholder directory structure. No features, no auth, no data — just a project that boots, builds, and lints cleanly.

## Questions to resolve before starting

None — all decisions documented in `BUILD_PLAN.md` section 3 (repo structure).

## Files to create

**Create at repo root:**
- `package.json` (see BUILD_PLAN.md Appendix H for the dependency skeleton)
- `tsconfig.json`
- `next.config.ts`
- `tailwind.config.ts`
- `postcss.config.js`
- `.gitignore` (Next.js defaults + `.env.local` + `supabase/.branches`)
- `.eslintrc.json`
- `.prettierrc`
- `components.json` (for shadcn/ui)

**Create in `src/app/`:**
- `src/app/layout.tsx` — minimal root layout
- `src/app/page.tsx` — placeholder landing ("Launchpad")
- `src/app/globals.css` — Tailwind directives + CSS variables from shadcn init

**Create placeholder directories** (with `.gitkeep` files) matching `BUILD_PLAN.md` section 3:
- `src/app/(marketing)/`, `src/app/(auth)/`, `src/app/(app)/`, `src/app/(admin)/`, `src/app/api/`
- `src/components/ui/`, `src/components/marketing/`, `src/components/auth/`, `src/components/lessons/`, `src/components/grading/`, `src/components/admin/`
- `src/lib/`, `src/actions/`, `src/types/`
- `supabase/`, `tests/unit/`, `tests/integration/`, `tests/e2e/`, `tests/fixtures/`
- `public/templates/`

## Acceptance criteria

- [ ] `pnpm install` completes without errors
- [ ] `pnpm dev` serves a blank "Launchpad" landing page at http://localhost:3000
- [ ] `pnpm typecheck` passes
- [ ] `pnpm lint` passes
- [ ] `pnpm build` completes successfully
- [ ] A Tailwind utility class applied to the root page renders correctly (verify in browser)
- [ ] `pnpm dlx shadcn@latest add button` succeeds; the button renders on the landing page
- [ ] All placeholder directories exist in the repo (verify with `git status` after adding .gitkeeps)

## Tests required

None yet. Test infrastructure comes in FND-004 and FND-005. Do not add test files in this ticket.

## Definition of done

- [ ] All acceptance criteria checked
- [ ] Typecheck clean (`pnpm typecheck`)
- [ ] Lint clean (`pnpm lint`)
- [ ] Build succeeds (`pnpm build`)
- [ ] Committed and pushed to branch `tkt/FND-001-init-nextjs`
- [ ] PR opened against `main` with acceptance criteria as a checklist in the body

## Suggested kickoff prompt

```
Read @CLAUDE.md and @docs/tickets/FND-001.md. Read @BUILD_PLAN.md sections 3 and
Appendix H (package.json).

Do NOT write code yet. Propose a plan covering:
1. The exact set of files you will create (full list)
2. The Next.js version you'll use (confirm 15.x)
3. Whether to use `create-next-app` or hand-write the config
4. Any questions the ticket does not answer

Wait for my approval before writing any code.
```
