# FND-005: Playwright E2E setup

**Milestone:** M1 Foundation
**Dependencies:** FND-001
**Estimated session length:** short (≤30 min)
**Status:** not started

## Context

E2E harness. Chromium only for MVP — no Firefox, no WebKit. One smoke test verifies the landing page renders and shows "Launchpad".

## Files to create

- `playwright.config.ts` — Chromium only, headless, dev server auto-start
- `tests/e2e/landing.spec.ts` — smoke test for landing page

## Acceptance criteria

- [ ] `pnpm test:e2e` runs the landing smoke test against a running dev server
- [ ] Playwright installs Chromium only (not all browsers)
- [ ] Test runs headless without manual input (CI-friendly)

## Tests required

- [ ] E2E: `tests/e2e/landing.spec.ts` — visit `/`, assert page title contains "Launchpad"

## Definition of done

- [ ] All acceptance criteria checked
- [ ] E2E test passes locally
- [ ] `pnpm test:e2e` added to `package.json` scripts
- [ ] PR opened
