# FND-006: GitHub Actions CI

**Milestone:** M1 Foundation
**Dependencies:** FND-004, FND-005
**Estimated session length:** short (≤30 min)
**Status:** not started

## Context

Run typecheck, lint, unit tests, and build on every PR and push to main. Skip E2E in CI for now (would need headed-browser job; defer to post-MVP). Cache pnpm store to keep runs under 3 minutes.

## Files to create

- `.github/workflows/ci.yml`

## Acceptance criteria

- [ ] Opening a PR triggers the CI workflow
- [ ] CI fails on typecheck error (verify by committing a deliberate type error on a throwaway branch, then reverting)
- [ ] CI fails on lint error
- [ ] CI fails on unit test failure
- [ ] pnpm store is cached across runs
- [ ] Full CI run completes in under 3 minutes

## Tests required

None new. This ticket verifies existing tests run in CI.

## Definition of done

- [ ] All acceptance criteria checked
- [ ] A test PR has been opened and the workflow has passed
- [ ] PR opened for this ticket
