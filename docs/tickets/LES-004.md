# LES-004: Video player (Bunny Stream) + lesson_progress table

**Milestone:** M3 Lesson Delivery
**Dependencies:** LES-003
**Estimated session length:** medium (30–90 min)
**Status:** not started

## Context

Bunny Stream iframe embed with progress telemetry to `lesson_progress`. MVP accepts any valid Bunny library ID and video GUID — real lesson footage is produced separately. The player posts watch-position every 10s and on pause.

## Questions to resolve before starting

- [ ] Bunny Stream account created? Library ID available (`BUNNY_STREAM_LIBRARY_ID`)?
- [ ] At minimum a placeholder video uploaded to test playback?
- [ ] Does the chosen Bunny library allow `<iframe>` embed (vs signed URLs)? MVP assumes the simpler iframe flow; signed URLs come later if we need DRM

## Files to create

- `supabase/migrations/20260106_lesson_progress.sql` — `lesson_progress` table (user_id, lesson_id, video_seconds_watched, video_duration, video_watched bool, updated_at), RLS for own rows, unique on (user_id, lesson_id)
- `src/components/lessons/video-player.tsx` — Bunny iframe + postMessage listener
- `src/actions/progress.ts` — `updateVideoProgress(lessonId, seconds, duration)` server action, throttled by the client
- `tests/unit/video-progress-action.test.ts`

## Acceptance criteria

- [ ] Video plays from `https://iframe.mediadelivery.net/embed/{LIBRARY_ID}/{VIDEO_GUID}`
- [ ] Watch-position posts every 10 seconds while playing (throttled)
- [ ] `video_watched=true` flips when cumulative watched ≥ 90% of duration
- [ ] Reloading the page resumes from last reported position (iframe seek on load)
- [ ] RLS: user can only write their own `lesson_progress` row

## Tests required

- [ ] Unit: `tests/unit/video-progress-action.test.ts` — RLS scope, 90% threshold flip, throttling behavior

## Definition of done

- [ ] Acceptance criteria checked
- [ ] Migration applied to prod Supabase
- [ ] Manually verified on preview with a real test video
- [ ] PR merged
