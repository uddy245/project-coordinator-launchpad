# PAY-003: Dashboard gate — no access = upsell

**Milestone:** M2 Auth & Checkout
**Dependencies:** PAY-002
**Estimated session length:** short (≤30 min)
**Status:** not started

## Context

Dashboard page checks `has_access`. If false, shows upsell (hero + `<PurchaseCTA />`). If true, shows the Lesson 20 card with progress. This ticket caps M2 — after this, M3 (lesson delivery) can start.

## Files to create/modify

- `src/app/(app)/dashboard/page.tsx` — server component: read profile, branch on `has_access`
- `src/components/marketing/upsell-hero.tsx` — no-access view
- `src/components/lessons/lesson-card.tsx` — placeholder for now (real lesson data in M3)
- `tests/e2e/dashboard-gate.spec.ts`

## Acceptance criteria

- [ ] `has_access=false` renders upsell with pricing and `<PurchaseCTA />`
- [ ] `has_access=true` renders the lesson dashboard (placeholder lesson card is fine)
- [ ] No flashing content on auth resolution — rendered server-side with the user's profile, no client-side flash between states
- [ ] Logged-out users hitting `/dashboard` are still redirected (AUTH-003 gate still enforced)

## Tests required

- [ ] E2E: `tests/e2e/dashboard-gate.spec.ts` — seeded user without access sees upsell; with access sees lesson card

## Definition of done

- [ ] Acceptance criteria checked
- [ ] `pnpm test` and `pnpm typecheck` and `pnpm lint` clean
- [ ] Manually verified on preview with two seeded users
- [ ] PR merged
